export interface EmailProps {
  email?: string;
  firstName: string;
  amount: number;
  serviceName: string;
}
export interface EventTicketEmailProps {
  email?: string;
  firstName: string;
  amount: number;
  eventName: string;
}

